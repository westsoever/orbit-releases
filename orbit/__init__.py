"""Orbit — local-first context capture and task detection.

``__version__`` is the single source of truth: ``pyproject.toml`` declares the
version dynamic and reads this attribute at build time.
"""

__version__ = "0.1.0"
